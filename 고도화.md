1. 프로젝트 Firebase 구성 요약
항목값Firebase SDKv12.9.0 (Modular SDK)사용 서비스Auth, Firestore, Storage인증 방식Google OAuth (Popup + Redirect 폴백)DB 패턴IndexedDB(Dexie) ↔ Firestore 양방향 실시간 동기화컬렉션 수7개 사용자 범위 + 1개 공개(sharedTrips)복합 인덱스3개 (trips, plans, places)Emulator 지원Auth:9099, Firestore:8080, Storage:9199

2. 발견된 버그 및 에러 (심각도 순)
BUG-01: sharing.ts — 에러 핸들링 부재 [심각도: HIGH]
파일: src/services/sharing.ts:120, 149
shareTrip()과 unshareTrip()에서 Firestore setDoc() / deleteDoc() 호출 시 try-catch가 없음.
Line 120: await setDoc(sharedDocRef, sharedData)   // ← 에러 핸들링 없음
Line 149: await deleteDoc(sharedDocRef)              // ← 에러 핸들링 없음
영향: 네트워크 오류, 권한 오류 시 사용자에게 피드백 없이 실패. shareTrip의 경우 로컬에 shareId가 저장되지만(line 123) Firestore 문서 생성이 실패하면 데드 링크 발생.
수정 방향:

setDoc/deleteDoc를 try-catch로 감싸고, 실패 시 로컬 상태 롤백
호출자에게 에러를 전파하여 UI에서 토스트 알림 표시


BUG-02: sharing.ts:120 — setDoc 실패 시 로컬-원격 불일치
파일: src/services/sharing.ts:117-124
Line 118: const firestore = getFirebaseDb()
Line 119: const sharedDocRef = doc(firestore, 'sharedTrips', shareId)
Line 120: await setDoc(sharedDocRef, sharedData)    // ← 여기서 실패하면
Line 123: await db.updateTrip(tripId, { shareId })  // ← 이것도 실행 안 됨 (OK)
사실 순서가 올바르므로 Firestore 실패 시 로컬도 업데이트 안 됨. 하지만 에러가 caller까지 전파되지 않아 UI에서 "공유 완료"로 잘못 표시될 수 있음.

BUG-03: firestoreSync.ts — resolveLocalTripId N+1 쿼리 [심각도: HIGH]
파일: src/services/firestoreSync.ts:2365-2369
typescriptprivate async resolveLocalTripId(tripFirebaseId: string): Promise<number | null> {
    const trip = await database.getTripByFirebaseId(tripFirebaseId)
    return trip?.id ?? null
}
이 함수가 루프 내부에서 호출됨:

syncPlansInitial() line 857 — 모든 원격 plan마다 1회
syncRouteSegmentsInitial() line 1077
syncTravelLogsInitial() line 1169
syncExpensesInitial() line 1292
실시간 리스너 콜백 lines 1494, 1748, 1792, 1867

영향: Plan 50개 + Log 100개 + Expense 200개 = 350+ IndexedDB 쿼리가 순차 실행. 초기 동기화가 수 초 지연될 수 있음.
수정 방향: 동기화 시작 시 tripFirebaseId → localId 매핑을 한 번에 구축하여 캐시로 사용

BUG-04: firestoreSync.ts — mergeInProgress 중 실시간 업데이트 드롭 [심각도: MEDIUM]
파일: src/services/firestoreSync.ts:1369, 1481, 1600, ...
typescriptconst tripUnsub = onSnapshot(tripsRef, async (snapshot) => {
    if (this.mergeInProgress) return   // ← 초기 동기화 중 모든 변경 무시
mergeInProgress가 true인 동안 (초기 동기화 전체 시간) 다른 기기에서 발생한 실시간 변경사항이 완전히 드롭됨. onSnapshot은 누적되지 않고 최신 스냅샷만 전달하므로 docChanges()에 포함되지 않은 변경은 영구 손실.
영향: 초기 동기화가 30초 걸리는 동안 다른 기기에서 수정한 내용이 누락될 수 있음.
수정 방향:

mergeInProgress 중 변경사항을 큐에 저장, 동기화 완료 후 재처리
또는 mergeInProgress 해제 직후 전체 스냅샷 비교


BUG-05: firestoreSync.ts — 배치 커밋 실패 시 데이터 유실 가능 [심각도: MEDIUM]
파일: src/services/firestoreSync.ts:1967-1984
typescripttry {
    await batch.commit()
} catch (error) {
    // Fallback: try individual writes
    for (const op of chunk) {
        try { ... } catch (e) {
            console.error('[Sync] Individual write fallback also failed:', e)
            // ← 실패한 op가 조용히 사라짐. 재시도 큐 없음.
        }
    }
}
영향: 개별 쓰기 폴백도 실패하면 해당 데이터가 영구 미동기화 상태. 사용자는 인지 불가.
수정 방향: 실패한 ops를 별도 큐에 저장하고 다음 flush 시 재시도, 또는 사용자에게 알림

BUG-06: firestoreSync.ts — 이미지 다운로드 fire-and-forget 패턴 [심각도: LOW-MEDIUM]
파일: src/services/firestoreSync.ts:1385-1389, 1423-1427, 1501-1506, etc.
typescriptimport('@/services/imageSync').then(({ downloadTripCoverImage }) => {
    dexieDb.trips.where('firebaseId').equals(docId).first().then((t) => {
        if (t) downloadTripCoverImage(t).then(() => this.notifyUpdateDebounced())
    })
}).catch(console.error)
문제점:

동적 import → DB 쿼리 → 다운로드 → notify가 3단 중첩 Promise 체인으로 어느 단계에서 실패해도 UI에 반영 안 됨
같은 이미지에 대해 중복 다운로드 요청 가능 (빠른 연속 변경 시)
.then() 내부의 에러가 외부 .catch(console.error)에 잡히지 않을 수 있음


BUG-07: authStore.ts — getRedirectResult 결과 미처리 [심각도: LOW]
파일: src/stores/authStore.ts:50-55
typescriptgetRedirectResult(auth).catch((error) => {
    if (error) {
        console.error('[Auth] Redirect result error:', error)
        set({ error: (error as Error).message })
    }
})
getRedirectResult의 성공 결과(UserCredential)를 처리하지 않음. 실제로는 onAuthStateChanged가 처리하므로 기능적 문제는 없지만, 리다이렉트 인증 후 isLoading이 true로 남아있을 수 있는 타이밍 이슈 존재.

BUG-08: firestoreSync.ts — plansCount 순차 업데이트 [심각도: LOW]
파일: src/services/firestoreSync.ts:1588-1591
typescriptfor (const tripId of affectedTripIds) {
    const count = await dexieDb.plans.where('tripId').equals(tripId).count()
    await dexieDb.trips.update(tripId, { plansCount: count })
}
```

**영향**: affectedTripIds가 여러 개일 때 순차 실행. `Promise.all()`로 병렬화 가능.

---

## 3. 보안 취약점

### SEC-01: Storage Rules — SVG 업로드 허용 [심각도: HIGH]
**파일**: `storage.rules:12`
```
request.resource.contentType.matches('image/.*')
```

`image/svg+xml`이 매칭됨. SVG는 `<script>` 태그를 포함할 수 있어 **XSS 공격** 벡터.

**수정 방향**:
```
request.resource.contentType.matches('image/(jpeg|png|webp|heic|gif)')
```

---

### SEC-02: sharedTrips — 비인증 전체 읽기 허용 [심각도: MEDIUM]
**파일**: `firestore.rules:37`
```
allow read: if true;
```

의도적 설계이지만:
- shareId가 12자리 영숫자(base36) → 브루트포스에 취약하진 않음
- 그러나 **열거 공격(enumeration)** 가능: `collection('sharedTrips')`에 대한 listDocuments 호출
- 만료 메커니즘 없음 → 한번 공유된 여행은 영구 공개

**수정 방향**:
- `expiresAt` 필드 추가 및 규칙에서 만료 검증
- shareId 길이를 16자 이상으로 증가
- Firestore 규칙에서 단일 문서 읽기만 허용 (list 차단)

---

### SEC-03: sharedTrips create — userId 검증 부재 [심각도: MEDIUM]
**파일**: `firestore.rules:38-39`
```
allow create: if request.auth != null
    && request.resource.data.keys().hasAll(['userId', 'tripData', 'createdAt']);
```

`request.resource.data.userId == request.auth.uid` 검증이 없음. 인증된 사용자가 다른 사용자의 uid로 문서를 생성할 수 있음.

**수정 방향**:
```
allow create: if request.auth != null
    && request.resource.data.userId == request.auth.uid
    && request.resource.data.keys().hasAll(['userId', 'tripData', 'createdAt']);

참고: 실제 sharing.ts line 114의 sharedData 구조에는 tripData 키가 없고 trip + plans 구조임.
즉, 현재 규칙이 클라이언트 데이터와 불일치. keys().hasAll(['userId', 'tripData', 'createdAt'])는 실제 데이터에 tripData가 없으므로 create가 실패해야 함.

SEC-03-CRITICAL: 보안 규칙과 클라이언트 데이터 불일치!
파일: firestore.rules:39 vs src/services/sharing.ts:92-115
Firestore 규칙은 tripData 키를 요구하지만, 클라이언트는 trip + plans 키를 전송:
typescript// sharing.ts line 92-115: 실제 전송 데이터
{
    trip: { ... },          // ← 'tripData'가 아닌 'trip'
    plans: [ ... ],
    sharedAt: Timestamp,
    sharedBy: uid           // ← 'userId'가 아닌 'sharedBy'
}
규칙이 요구하는 키: ['userId', 'tripData', 'createdAt']
실제 전송하는 키: ['trip', 'plans', 'sharedAt', 'sharedBy']
이 불일치로 인해 두 가지 가능성:

공유 기능이 완전히 작동하지 않음 (가장 가능성 높음) — create 규칙 위반으로 항상 PERMISSION_DENIED
또는 배포된 규칙이 소스와 다름 (firebase deploy 이후 규칙 수정됨)

검증 필요: firebase emulators에서 shareTrip() 실행 테스트

SEC-04: 데이터 크기 제한 없음 [심각도: LOW]
파일: firestore.rules:10-32
사용자 데이터에 문서 크기, 필드 수, 컬렉션 내 문서 수 제한이 없음. Firestore 자체의 1MB 문서 제한은 있지만, 악의적 사용자가 수만 개의 문서를 생성할 수 있음.

4. 성능 문제 및 개선 방안
PERF-01: 초기 동기화 — 페이지네이션 없는 전체 컬렉션 읽기 [우선순위: CRITICAL]
파일: src/services/firestoreSync.ts:753, 838, 933, 1058, 1140, 1273
typescriptwithRetry(() => getDocs(tripsRef), 'Trips fetch')    // 모든 trips
withRetry(() => getDocs(plansRef), 'Plans fetch')     // 모든 plans
withRetry(() => getDocs(placesRef), 'Places fetch')   // 모든 places
// ... segmentsRef, logsRef, expensesRef도 동일
6개 컬렉션 전체를 limit() 없이 읽음.
비용 영향:

사용자당 여행 50개, 일정 500개, 장소 200개, 로그 1000개 기준
로그인 시 ~1,750 Firestore 읽기 발생
Firestore 가격: 100K 읽기당 $0.06
월 활성 사용자 1만명 × 일 1회 로그인: 월 $105
데이터가 증가하면 비용 선형 증가

응답 크기 문제:

Firestore getDocs는 최대 10MB 또는 1000문서 제한 없이 반환
실제로는 gRPC 레벨에서 약 10MB 응답 제한이 있어 대량 데이터 시 타임아웃

개선 방안:

limit() + 커서 기반 페이지네이션
updatedAt > lastSyncTimestamp 조건으로 변경분만 가져오기 (증분 동기화)
초기 동기화 시 메타데이터만 먼저 가져오고, 상세 데이터는 필요 시 로드 (지연 로드)


PERF-02: resolveLocalTripId — 반복 DB 조회 캐싱 부재 [우선순위: HIGH]
파일: src/services/firestoreSync.ts:2365-2369
초기 동기화와 실시간 리스너 모두에서 매번 IndexedDB 쿼리 실행.
개선 방안:
typescript// 동기화 시작 시 한 번만 구축
private tripIdCache = new Map<string, number>()

private async buildTripIdCache(): Promise<void> {
    const trips = await dexieDb.trips.toArray()
    this.tripIdCache.clear()
    for (const trip of trips) {
        if (trip.firebaseId && trip.id) {
            this.tripIdCache.set(trip.firebaseId, trip.id)
        }
    }
}
예상 개선: 350+ DB 쿼리 → 1회 쿼리로 99.7% 감소

PERF-03: Firestore 오프라인 지속성 미활성화 [우선순위: HIGH]
파일: src/services/firebase.ts
Firestore의 내장 오프라인 캐시가 활성화되지 않음. 현재는 IndexedDB(Dexie)가 오프라인을 담당하지만, Firestore 오프라인 지속성을 활성화하면:

네트워크 끊김 시 Firestore 쓰기가 자동 큐잉
재연결 시 자동 동기화
첫 로드 시 캐시된 데이터로 즉시 표시

주의: 이미 IndexedDB 기반 동기화를 구현했으므로, Firestore 오프라인 캐시와 충돌 가능성이 있음. 신중한 검토 필요.
개선 방안 (Firebase v10+ 모듈):
typescriptimport { initializeFirestore, persistentLocalCache, persistentMultipleTabManager } from 'firebase/firestore'

// getFirestore() 대신:
const db = initializeFirestore(app, {
    localCache: persistentLocalCache({
        tabManager: persistentMultipleTabManager()
    })
})

PERF-04: 이미지 동기화 — 순차적 업로드/다운로드 [우선순위: MEDIUM]
파일: src/services/imageSync.ts:101-113, 139-147, 164-177, 202-209
typescript// Plan photos - 순차 업로드
for (let i = 0; i < plan.photos.length; i++) {
    await uploadImageToStorage(storagePath, photo)  // ← 하나씩 순차 실행
}

// Plan photos - 순차 다운로드
for (const path of plan.photoPaths) {
    const base64 = await downloadImageFromStorage(path)  // ← 하나씩 순차 실행
}
syncAllImagesBackground()은 MAX_CONCURRENT = 3으로 엔티티 수준에서는 병렬이지만, 개별 엔티티 내 사진들은 순차 처리.
개선 방안:
typescript// 각 엔티티 내에서도 Promise.all로 병렬화
const results = await Promise.all(
    plan.photos.map((photo, i) => uploadImageToStorage(paths[i], photo))
)

PERF-05: 이미지 다운로드 중복 DB 조회 [우선순위: MEDIUM]
파일: src/services/firestoreSync.ts:1386, 1424, 1502, etc.
실시간 리스너에서 이미지 다운로드 시 같은 문서를 다시 DB에서 조회:
typescript// 이미 local 변수에 데이터가 있는데
const local = await database.getTripByFirebaseId(docId)  // line 1378

// 이미지 다운로드 시 다시 조회
dexieDb.trips.where('firebaseId').equals(docId).first().then((t) => {  // line 1386
개선 방안: 외부 스코프의 local 변수 또는 방금 추가/업데이트한 ID를 클로저로 전달

PERF-06: 동기화 쿨다운이 sessionStorage 기반 [우선순위: LOW]
파일: src/services/firestoreSync.ts:422-430
typescriptprivate isSyncFresh(userId: string): boolean {
    const stored = sessionStorage.getItem(key)
    return Date.now() - parseInt(stored, 10) < SyncManager.SYNC_COOLDOWN_MS
}

탭을 닫으면 sessionStorage 초기화 → 다시 전체 동기화
동기화 완료 시점이 아닌 시작 시점에 마크되면 불완전 동기화로도 쿨다운 적용

실제 코드를 보면 markSyncComplete가 초기 동기화 성공 후 호출되므로 (line 519) 후자 문제는 없음.

PERF-07: 중복 Firestore 인스턴스 생성 가능성 [우선순위: LOW]
파일: src/services/firebase.ts:85-89 vs src/pages/SharedTrip.tsx:84
SharedTrip 페이지에서 getFirebaseDb()를 직접 호출하고, Firestore SDK를 직접 import:
typescript// SharedTrip.tsx
import { doc, getDoc, Timestamp } from 'firebase/firestore'
const db = getFirebaseDb()
싱글톤 패턴이 잘 적용되어 있으므로 실제 중복 인스턴스는 생성되지 않지만, SharedTrip은 인증 없이 접근하는 공개 페이지이므로 Firebase 전체 SDK가 로드됨.
개선 방안: SharedTrip 페이지를 code-splitting으로 분리하여 메인 번들 크기 최적화

5. 누락된 인덱스
IDX-01: expenses 컬렉션 인덱스 부재
파일: firestore.indexes.json
expenses 컬렉션에 대한 복합 인덱스가 없음. 만약 expenses를 tripFirebaseId + date 등으로 쿼리한다면 인덱스가 필요.
현재 코드에서는 getDocs(expensesRef)로 전체를 가져오므로 인덱스가 필수는 아니지만, 향후 쿼리 최적화 시 필요.
IDX-02: travelLogs / routeSegments 컬렉션 인덱스 부재
동일하게 travelLogs, routeSegments에도 복합 인덱스 없음.

6. 구조적/아키텍처 이슈
ARCH-01: firestoreSync.ts 파일 크기 (2379줄)
단일 파일에 동기화 로직, 데이터 변환, 배치 큐, 리스너 관리, ID 매핑이 모두 포함됨. 유지보수와 테스트가 어려움.
개선 방향: 관심사 분리

converters.ts — Firestore ↔ Local 데이터 변환
syncInitial.ts — 초기 동기화 로직
syncListeners.ts — 실시간 리스너
syncBatch.ts — 배치 쓰기 큐
syncManager.ts — 오케스트레이션

ARCH-02: sharing.ts 와 firestore.rules 키 불일치
SEC-03-CRITICAL에서 기술. 클라이언트와 보안 규칙이 서로 다른 필드명 사용.

7. 우선순위별 개선 로드맵
즉시 수정 필요 (Week 1)
#이슈예상 소요파일1SEC-03-CRITICAL: 규칙/클라이언트 필드 불일치 확인30분firestore.rules + sharing.ts2SEC-01: SVG 업로드 차단5분storage.rules3BUG-01: sharing.ts 에러 핸들링 추가15분sharing.ts4SEC-03: sharedTrips create에 userId 검증 추가5분firestore.rules
성능 개선 (Week 2)
#이슈예상 효과파일5PERF-02: tripId 캐시 구현DB 쿼리 99% 감소firestoreSync.ts6BUG-08: plansCount 병렬화리스너 응답속도 개선firestoreSync.ts7PERF-05: 이미지 다운로드 중복 조회 제거DB 쿼리 50% 감소firestoreSync.ts
중기 개선 (Week 3-4)
#이슈예상 효과파일8PERF-01: 증분 동기화 구현Firestore 읽기 80% 감소firestoreSync.ts9BUG-04: mergeInProgress 중 변경 큐잉데이터 무결성 보장firestoreSync.ts10BUG-05: 배치 실패 재시도 큐데이터 동기화 신뢰성firestoreSync.ts11PERF-04: 이미지 병렬 업/다운로드이미지 동기화 2-3x 빠름imageSync.ts
장기 개선 (선택)
#이슈비고12ARCH-01: firestoreSync.ts 분리유지보수성 대폭 향상13PERF-03: Firestore 오프라인 지속성Dexie와 충돌 가능성 검토 필요14SEC-02: sharedTrips 만료 메커니즘프라이버시 강화

8. 검증 방법
각 이슈 확인을 위한 테스트:

SEC-03-CRITICAL 검증: Firebase Emulator에서 shareTrip() 호출 → PERMISSION_DENIED 발생 여부 확인
BUG-03 성능 측정: Performance 탭에서 초기 동기화 시간 측정 (데이터 50+ 기준)
BUG-04 재현: 두 기기 동시 사용, 초기 동기화 중 다른 기기에서 수정 → 누락 여부 확인
SEC-01 검증: SVG 파일 업로드 시도 → Storage 규칙이 허용하는지 확인
PERF-01 비용 확인: Firebase Console → Usage 탭에서 일일 읽기 수 확인