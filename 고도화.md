


Context
현재 Moonwave Travel v4.0에는 "여행 기록" (TravelLog) 기능이 있으며, 영수증 OCR과 사진/메모의 간편 경비 입력을 통해 경비 데이터를 수집하고 있다. 그러나 경비 데이터를 전문적으로 관리·분석하는 독립 기능이 없어, 사용자가 여행 경비를 체계적으로 파악하기 어렵다.

목표: 여행기록과 동일한 UI/UX 패턴으로 "여행 경비" 전용 페이지를 구축하여, 기존 TravelLog 경비 데이터를 기반으로 세부 카테고리 분류·직접 입력·분석·예산 관리·내보내기 기능을 제공한다.

Phase 1: Data Foundation
1.1 타입 정의 (src/types/index.ts)
추가할 타입:

typescript
// 세부 카테고리
type TransportSubCategory = 'flight' | 'taxi' | 'subway' | 'bus' | 'rental_car'
type FoodSubCategory = 'restaurant' | 'cafe' | 'convenience' | 'delivery' | 'snack'
type AccommodationSubCategory = 'hotel' | 'airbnb' | 'hostel' | 'guesthouse'
type ShoppingSubCategory = 'souvenir' | 'clothing' | 'dutyfree' | 'mart'
type AttractionSubCategory = 'admission' | 'experience' | 'tour'
type OtherSubCategory = 'telecom' | 'insurance' | 'exchange_fee' | 'laundry' | 'tip'

type ExpenseSubCategory =
  | TransportSubCategory | FoodSubCategory | AccommodationSubCategory
  | ShoppingSubCategory | AttractionSubCategory | OtherSubCategory

// 독립 경비 레코드
interface Expense {
  id?: number
  firebaseId?: string
  tripId: number
  tripFirebaseId?: string
  day: number
  timestamp: string
  category: ExpenseCategory
  subCategory?: ExpenseSubCategory
  storeName: string
  memo?: string
  items: ExpenseItem[]          // 기존 ExpenseItem 재사용
  totalAmount: number
  currency: string
  receiptDate?: string
  sourceLogId?: number          // TravelLog.id FK (자동 import 시)
  sourceLogFirebaseId?: string
  latitude?: number
  longitude?: number
  address?: string
  paidBy?: string               // 정산용 (Phase 4)
  participants?: string[]       // 정산용 (Phase 4)
  createdAt: Date
  updatedAt: Date
}

// 여행 예산
interface TripBudget {
  totalBudget: number
  budgetCurrency: string
  categoryBudgets?: Partial<Record<ExpenseCategory, number>>
}
```

**수정:** `Trip` 인터페이스에 `budget?: TripBudget` 필드 추가

### 1.2 상수 정의 (`src/utils/constants.ts`)

**추가할 상수:**
- `EXPENSE_SUBCATEGORY_LABELS`: 세부 카테고리 한국어 라벨 (flight→항공, taxi→택시 등 23개)
- `EXPENSE_SUBCATEGORY_MAP`: 상위→하위 카테고리 매핑 (transport→[flight,taxi,...] 등)
- `EXPENSE_SUBCATEGORY_ICONS`: 세부 카테고리별 lucide 아이콘 매핑

### 1.3 데이터베이스 (`src/services/database.ts`)

**DB 스키마 v6 추가:**
```
expenses: '++id, tripId, day, category, timestamp, [tripId+day], firebaseId, tripFirebaseId, sourceLogId'
Expense CRUD 함수 추가 (기존 travelLogs 패턴 준수):

getExpensesForTrip(tripId) / getExpensesForTripDay(tripId, day)
getExpense(id) / getExpenseByFirebaseId(firebaseId)
addExpense() / updateExpense() / deleteExpense()
getExpenseBySourceLogId(sourceLogId) — import 중복 방지
importExpensesFromTravelLogs(tripId) — TravelLog 경비 → Expense 테이블 import
deleteExpensesForTrip(tripId) — Trip 삭제 시 cascade
import 로직:

tripId의 모든 travelLogs 중 expense != null인 항목 조회
각 log에 대해 sourceLogId로 이미 import 여부 확인
미등록 시 Expense 레코드 생성 (모든 필드 매핑)
import된 건수 반환
1.4 Zustand Store (src/stores/expenseStore.ts — 신규)
travelLogStore.ts 패턴 100% 준수:

typescript
interface ExpenseState {
  expenses: Expense[]
  isLoading: boolean
  error: string | null

  loadExpenses: (tripId: number) => Promise<void>
  addExpense: (expense: Omit<Expense, 'id' | 'createdAt' | 'updatedAt'>) => Promise<number>
  updateExpense: (id: number, updates: Partial<Expense>) => Promise<void>
  deleteExpense: (id: number) => Promise<void>  // 30초 undo 포함
  importFromTravelLogs: (tripId: number) => Promise<number>
  clearExpenses: () => void
}
Firestore sync (syncManager 연동)
30초 undo delete (travelLogStore 동일 패턴)
broadcast 이벤트 (EXPENSE_CREATED, EXPENSE_UPDATED, EXPENSE_DELETED)
selector hooks: useExpenses, useExpenseLoading
1.5 Firestore Sync (src/services/firestoreSync.ts)
travelLog 동기화 패턴 준수하여 추가:

expenseToFirestore() / firestoreToExpenseData()
uploadExpense() / deleteRemoteExpense()
startListening() 내 expense 리스너 추가
Phase 2: Core UI
2.1 라우팅 (src/main.tsx)
typescript
const TravelExpensePage = lazy(() => import('@/pages/TravelExpense').then(m => ({ default: m.TravelExpense })))

// router 설정에 추가 (trips/:id/log/map 라우트 다음):
{ path: 'trips/:id/expenses', element: <Suspense fallback={<PageLoading />}><TravelExpensePage /></Suspense> }
2.2 네비게이션 진입점 (src/pages/TripDetail.tsx)
모바일 아이콘 그리드 (line ~313, grid-cols-6에 맞게):

기존 5개 (추가, 지도, 내비, 기록, AI) → 기록과 AI 사이에 "경비" 추가
아이콘: Wallet (lucide-react)
데스크톱 인라인 버튼 (line ~353):

"여행 기록" 버튼 다음에 "여행 경비" 버튼 추가
2.3 View Hook (src/hooks/useExpenseView.ts — 신규)
useTravelLogView.ts 패턴 준수:

typescript
interface UseExpenseViewParams {
  expenses: Expense[]
  totalDays: number
  sortOrder: 'newest' | 'oldest'
  categoryFilter: ExpenseCategory | null
  subCategoryFilter: ExpenseSubCategory | null
  searchQuery: string
  loadedDayCount: number
}

// Returns:
// sortedDays, visibleDays, expensesByDay, filteredExpensesByDay,
// dayTotals, dayCategoryTotals, hasMoreDays, totalFilteredCount,
// grandTotals, categoryGrandTotals, dailyAverage
```

### 2.4 컴포넌트 구조 (`src/components/expense/` — 신규 디렉토리)

| 파일 | 설명 | 참조 패턴 |
|------|------|-----------|
| `index.ts` | Barrel export | `travellog/index.ts` |
| `ExpenseCard.tsx` | 단일 경비 카드 | `TimelineCard.tsx` |
| `ExpenseDaySection.tsx` | 일별 아코디언 | `DaySection.tsx` |
| `ExpenseDaySectionHeader.tsx` | 일별 헤더+합계 | `DaySectionHeader.tsx` |
| `ExpenseEntryDialog.tsx` | 수동 경비 입력 폼 | `MemoLogInput.tsx` |
| `ExpenseEditDialog.tsx` | 경비 수정 모달 | `EditLogModal.tsx` |
| `ExpenseFilterBar.tsx` | 검색+카테고리+세부카테고리 필터 | `SearchFilterBar.tsx` |
| `ExpenseOverview.tsx` | 전체 경비 요약 (통화별/카테고리별) | `ExpenseSummary.tsx` |
| `ExpenseBudgetBar.tsx` | 예산 진행률 바 | 신규 |
| `ExpenseAnalytics.tsx` | 분석 대시보드 (차트) | 신규 |
| `ExpenseExportButton.tsx` | CSV 내보내기 버튼 | `ExportButton.tsx` |

### 2.5 메인 페이지 (`src/pages/TravelExpense.tsx` — 신규)

**레이아웃** (TravelLog.tsx 구조 미러링):
```
PageContainer
├── Header: ← 뒤로가기 | "여행 경비" | trip.title | [분석] [내보내기]
├── ExpenseOverview: 통화별 합계 + KRW 환산 + 카테고리 비율 바
├── ExpenseBudgetBar: 예산 대비 진행률 (예산 설정 시)
├── Sticky 컨트롤 바
│   ├── Day 탭 (가로 스크롤)
│   ├── 카테고리 필터 칩 (6개)
│   ├── 세부카테고리 칩 (카테고리 선택 시 표시)
│   └── 정렬 | 펼치기/접기 | 검색
├── ExpenseFilterBar (검색 활성화 시)
├── Day Sections (아코디언)
│   └── ExpenseDaySection
│       ├── ExpenseDaySectionHeader (일차, 날짜, 일별 합계)
│       └── ExpenseCard[] (경비 목록)
├── SpeedDialFAB: "경비 추가" 단일 액션
├── ScrollToTopFAB
├── DayMinimap (데스크톱)
├── ExpenseEntryDialog (수동 입력 모달)
└── ExpenseEditDialog (수정 모달)
```

### 2.6 ExpenseCard 레이아웃
```
[카테고리 아이콘 (8x8 원형)] | [시간] [가게명] [세부카테고리 뱃지]
                              | [금액 (통화 포함)] [KRW 환산]
                              | [메모] (있을 경우)
                              | [수정/삭제] (hover 시)
```
- 카테고리별 색상: 기존 `categoryColorClasses` 재사용
- 세부카테고리: Badge 컴포넌트로 표시

### 2.7 ExpenseEntryDialog 필드
```
DialogTitle: "경비 추가"
├── Day 선택 (1..totalDays)
├── 카테고리 칩 (6개, ExpenseSummary 스타일)
├── 세부카테고리 칩 (카테고리 선택 후 표시)
├── 가게명 (Input)
├── 금액 (Input number) + 통화 선택 (드롭다운)
├── 항목 섹션 (선택, 펼침형)
│   └── 항목 행: 이름, 수량, 단가, 금액
├── 메모 (Textarea, 선택)
├── 날짜 (date input, 기본값: 해당 일차 날짜)
└── [취소] [저장]
Phase 3: Analytics & 부가기능
3.1 예산 관리 (ExpenseBudgetBar.tsx)
Trip에 budget 설정 시 표시
진행률 바: 초록(<50%) → 노랑(50-80%) → 주황(80-100%) → 빨강(>100%)
잔여 예산 표시
"예산 설정" 버튼 (Dialog로 totalBudget + currency 입력)
3.2 분석 대시보드 (ExpenseAnalytics.tsx)
차트 라이브러리 없이 CSS 기반 구현 (ExpenseSummary의 바 차트 패턴 활용):

일별 지출 추이: 가로 바 차트 (일차별 금액, KRW 환산)
카테고리 분포: 비례 스택 바 또는 도넛 (CSS)
일평균 지출: 숫자 카드
최다 지출 카테고리/세부카테고리: 하이라이트 카드
전일 대비: 어제 vs 오늘 비교
3.3 CSV 내보내기 (src/utils/expenseExport.ts — 신규)
typescript
function generateExpenseCSV(expenses: Expense[], tripTitle: string): string
function downloadExpenseCSV(expenses: Expense[], tripTitle: string): void
CSV 컬럼: 날짜, 일차, 카테고리, 세부카테고리, 가게명, 항목, 금액, 통화, 메모, 주소

구현 순서 (파일별)
Step 1: 타입 + 상수
src/types/index.ts — Expense, ExpenseSubCategory, TripBudget 타입 추가
src/utils/constants.ts — 세부카테고리 라벨/아이콘/매핑 상수 추가
Step 2: 데이터 레이어
src/services/database.ts — v6 스키마, Expense CRUD, import 로직
src/stores/expenseStore.ts — 신규 Zustand store
Step 3: Firestore 동기화
src/services/firestoreSync.ts — expense 동기화 메서드 추가
Step 4: View Hook
src/hooks/useExpenseView.ts — 필터/정렬/페이지네이션 로직
Step 5: UI 컴포넌트 (하위 → 상위)
src/components/expense/ExpenseCard.tsx
src/components/expense/ExpenseDaySectionHeader.tsx
src/components/expense/ExpenseDaySection.tsx
src/components/expense/ExpenseFilterBar.tsx
src/components/expense/ExpenseOverview.tsx
src/components/expense/ExpenseEntryDialog.tsx
src/components/expense/ExpenseEditDialog.tsx
src/components/expense/ExpenseBudgetBar.tsx
src/components/expense/ExpenseAnalytics.tsx
src/components/expense/ExpenseExportButton.tsx
src/utils/expenseExport.ts — CSV 생성 유틸
src/components/expense/index.ts — barrel export
Step 6: 페이지 + 라우팅
src/pages/TravelExpense.tsx — 메인 페이지
src/main.tsx — 라우트 추가
src/pages/TripDetail.tsx — 네비게이션 버튼 추가
수정 vs 신규 파일
수정 (기존 파일, 7개)
파일	변경 내용
src/types/index.ts	Expense, ExpenseSubCategory, TripBudget 타입, Trip에 budget 필드
src/utils/constants.ts	세부카테고리 라벨/아이콘/매핑 상수
src/services/database.ts	v6 스키마, expenses 테이블, CRUD, import, cascade delete
src/services/firestoreSync.ts	expense 동기화 메서드
src/main.tsx	/trips/:id/expenses 라우트
src/pages/TripDetail.tsx	"경비" 네비게이션 버튼 (모바일+데스크톱)
src/stores/index.ts	expenseStore export 추가 (barrel에 있을 경우)
신규 (생성 파일, 14개)
파일	설명
src/stores/expenseStore.ts	Zustand 상태관리
src/hooks/useExpenseView.ts	View 로직 hook
src/pages/TravelExpense.tsx	메인 페이지
src/components/expense/index.ts	Barrel export
src/components/expense/ExpenseCard.tsx	경비 카드
src/components/expense/ExpenseDaySection.tsx	일별 아코디언
src/components/expense/ExpenseDaySectionHeader.tsx	일별 헤더
src/components/expense/ExpenseEntryDialog.tsx	수동 입력 폼
src/components/expense/ExpenseEditDialog.tsx	수정 모달
src/components/expense/ExpenseFilterBar.tsx	검색/필터 바
src/components/expense/ExpenseOverview.tsx	경비 요약
src/components/expense/ExpenseBudgetBar.tsx	예산 진행률
src/components/expense/ExpenseAnalytics.tsx	분석 대시보드
src/components/expense/ExpenseExportButton.tsx	CSV 내보내기
src/utils/expenseExport.ts	CSV 생성 유틸
재사용 컴포넌트/유틸 목록
기존 항목	위치	재사용 방식
PageContainer	components/layout	페이지 래퍼
Card, Button, IconButton	components/ui	모든 UI 요소
Dialog, DialogTitle, DialogBody, DialogActions	components/ui	Entry/Edit/Budget 다이얼로그
Input, Badge, Skeleton	components/ui	폼 필드, 뱃지, 로딩
SpeedDialFAB, ScrollToTopFAB	components/ui, components/travellog	FAB 버튼
DayMinimap	components/travellog	데스크톱 사이드바 (직접 재사용 또는 패턴 참조)
useExchangeRates	hooks/useExchangeRates.ts	KRW 환산
convertToKRW	services/exchangeRateService.ts	환산 계산
formatAmount 패턴	ExpenseSummary.tsx	통화 포맷팅
categoryColorClasses, categoryIcons	ExpenseSummary.tsx, DaySection.tsx	카테고리 색상/아이콘
EXPENSE_CATEGORY_LABELS, CURRENCY_SYMBOLS	utils/constants.ts	라벨/심볼
AnimatePresence, motion	framer-motion	아코디언 애니메이션
clsx	clsx	조건부 클래스
sendBroadcast	services/broadcast.ts	크로스탭 동기화
syncManager	services/firestoreSync.ts	Firestore 동기화
TravelLog→Expense 동기화 전략
경비 페이지 최초 진입 시: importFromTravelLogs(tripId) 자동 실행
중복 방지: sourceLogId로 이미 import된 항목 skip
업데이트 감지: import 시 sourceLogId가 존재하는 Expense의 updatedAt과 원본 TravelLog의 updatedAt 비교 → 차이가 있으면 업데이트
수동 입력 경비: sourceLogId = undefined로 독립 레코드
검증 방법
npm run typecheck — 타입 에러 없음 확인
npm run lint — 린트 통과
npm run build — 빌드 성공
npm run dev — 개발 서버 실행 후:
TripDetail에서 "경비" 버튼 표시 확인 (모바일/데스크톱)
/trips/:id/expenses 라우트 접근 확인
TravelLog에 등록된 경비 자동 import 확인
수동 경비 추가/수정/삭제 동작 확인
세부카테고리 필터링 동작 확인
예산 설정 및 진행률 표시 확인
분석 대시보드 데이터 표시 확인
CSV 내보내기 파일 생성 확인
KRW 환산 토글 동작 확인
