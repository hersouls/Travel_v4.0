

Context
Galaxy S24 (360×780px) 화면에서 전체 앱이 혼란스럽고 보기 어려운 문제. 버튼/아이콘 밀집, 텍스트 겹침/잘림, 한 화면에 정보 과다 등 복합적 문제가 모든 페이지와 컴포넌트에서 발생.

핵심 원인: Tailwind sm breakpoint(640px) 이하에서 적용되지 않는 반응형 클래스, 고정 그리드 열 수, 과도한 gap/padding, 컴포넌트 레벨의 밀집 레이아웃.

A. 기반 컴포넌트 수정 (모든 페이지에 영향)
A-1. Dialog 컴포넌트 — 모바일 전폭 + 패딩 축소
파일: src/components/ui/Dialog.tsx

현재 sizeStyles가 sm:max-w-*로만 정의되어 360px에서 무제한. 또한 p-6 패딩이 과다.

Line 19-26 — sizeStyles: sm:max-w-* → 모바일에서 max-w 없음
변경: 기본값으로 max-w-[calc(100vw-2rem)] 추가 (모바일 전폭 - 양옆 여백)
diff
 const sizeStyles = {
-  sm: 'sm:max-w-sm',
-  md: 'sm:max-w-md',
-  lg: 'sm:max-w-lg',
-  xl: 'sm:max-w-xl',
-  '2xl': 'sm:max-w-2xl',
-  full: 'sm:max-w-[calc(100vw-2rem)]',
+  sm: 'max-w-[calc(100vw-2rem)] sm:max-w-sm',
+  md: 'max-w-[calc(100vw-2rem)] sm:max-w-md',
+  lg: 'max-w-[calc(100vw-2rem)] sm:max-w-lg',
+  xl: 'max-w-[calc(100vw-2rem)] sm:max-w-xl',
+  '2xl': 'max-w-[calc(100vw-2rem)] sm:max-w-2xl',
+  full: 'max-w-[calc(100vw-2rem)]',
 }
```
```
Line 57 — DialogPanel: p-6 → p-4 sm:p-6
```
```
Line 116 — DialogActions: gap-3 → gap-2 sm:gap-3
```

---

### A-2. Lightbox 컴포넌트 — 모바일 패딩 축소
**파일**: `src/components/ui/Lightbox.tsx`
```
Line 137 — 이미지 컨테이너: p-12 sm:p-16 → p-4 sm:p-12 md:p-16
```

360px에서 `p-12`(48px×2=96px) 사용 시 이미지 영역이 264px로 축소됨. `p-4`로 변경하면 328px 확보.

---

### A-3. Header 컴포넌트 — 간격 축소
**파일**: `src/components/layout/Header.tsx`
```
Line 81 — 좌측 영역: gap-3 → gap-2 sm:gap-3
Line 123 — 우측 액션: gap-2 → gap-1.5 sm:gap-2
```

---

### A-4. PageContainer — 모바일 패딩 미세 축소 (선택적)
**파일**: `src/components/layout/PageContainer.tsx`
```
Line 42 — p-4 lg:p-6 → p-3 sm:p-4 lg:p-6
```

360px에서 16px→12px로 줄이면 콘텐츠 영역 328px→336px 확보 (8px 추가 확보).

---

## B. TravelLog 관련 컴포넌트 수정

### B-1. TimelineCard — 헤더 밀집 해소 + 영수증 사진 크기
**파일**: `src/components/travellog/TimelineCard.tsx`
```
Line 74 — 외부 flex: gap-3 → gap-2 sm:gap-3
Line 90 — 헤더: flex items-center gap-2 → flex items-center gap-1.5 sm:gap-2 flex-wrap
```

헤더에 시간, 위치, 고도, 내비, 방문횟수, 편집/삭제 버튼이 한 줄에 전부 있어 360px에서 넘침.
`flex-wrap` 추가로 2행 배치 허용.
```
Line 223 — 영수증 사진: w-32 h-40 → w-24 h-32 sm:w-32 sm:h-40
```

360px에서 128px→96px로 축소하여 텍스트 영역 확보.

---

### B-2. PhotoLogUploader — AI배너 스택화 + 썸네일 축소
**파일**: `src/components/travellog/PhotoLogUploader.tsx`
```
Line 330 — AI 배너: flex items-center justify-between gap-3
→ flex flex-col sm:flex-row items-start sm:items-center gap-2 sm:gap-3
```

360px에서 텍스트+2개 버튼이 한 줄에 불가능. 세로 스택으로 변경.
```
Line 335 — 버튼 그룹: flex items-center gap-2
→ flex items-center gap-1.5 sm:gap-2 flex-wrap
```
```
Line 365 — 진행률 배너: 동일하게 flex-col sm:flex-row 적용
```
```
Line 390 — 드래그 영역: p-6 → p-4 sm:p-6
```
```
Line 445 — 썸네일: size-24 → size-20 sm:size-24
```

96px→80px. flex 내에서 16px 절약.
```
Line 438 — 포토 카드: flex gap-3 p-3 → flex gap-2 sm:gap-3 p-2 sm:p-3
```

---

### B-3. EditLogModal — 그리드 단일열화 + 패딩 축소
**파일**: `src/components/travellog/EditLogModal.tsx`
```
Line 283 — Day/Time 그리드: grid grid-cols-2 gap-3 → grid grid-cols-1 sm:grid-cols-2 gap-2 sm:gap-3
```
```
Line 382 (있을 경우) — 영수증 경비 그리드: 동일하게 grid-cols-1 sm:grid-cols-2
```
```
Line 343 (있을 경우) — 영수증 썸네일: w-24 h-32 → w-20 h-28 sm:w-24 sm:h-32
```
```
Line 470 — 위치 버튼: gap-2 → gap-1.5 sm:gap-2
```

---

### B-4. ExpenseSummary — 이동통계 그리드 축소
**파일**: `src/components/travellog/ExpenseSummary.tsx`
```
Line 159 — 이동 통계: grid grid-cols-3 gap-3 → grid grid-cols-3 gap-1.5 sm:gap-3
```

각 셀 `p-2`는 유지. gap만 축소하면 360px에서 각 셀 ~108px→~112px.
```
Line 197 — 카테고리별 아이템: gap-3 → gap-2 sm:gap-3
```

---

### B-5. SearchFilterBar — 필터칩 gap 축소
**파일**: `src/components/travellog/SearchFilterBar.tsx`
```
Line 90 (부근) — 카테고리 칩: gap-1.5 → gap-1 sm:gap-1.5
```

---

### B-6. GridView — 확인 (이미 양호)
**파일**: `src/components/travellog/GridView.tsx`
```
Line 148 — grid-cols-2 sm:grid-cols-3 gap-2 → 유지 (360px에서 2열 적절)
```

---

## C. 페이지별 수정

### C-1. Settings.tsx — 그리드 열 수 전면 조정 + 탭바 + 제목
**파일**: `src/pages/Settings.tsx`
```
Line 335 — 제목: text-2xl → text-xl sm:text-2xl
Line 339 — 탭바: flex gap-1 → flex gap-0.5 sm:gap-1
Line 408 — 테마 3열: grid-cols-3 gap-3 → grid-cols-3 gap-2
       (테마 셀 패딩): p-4 → p-3 sm:p-4
Line 454 — 컬러 팔레트 5열: grid-cols-5 gap-2 → grid-cols-3 sm:grid-cols-5 gap-1.5 sm:gap-2
Line 535 — 지도 2열: grid-cols-2 gap-3 → grid-cols-2 gap-2
Line 563 — 이동수단 4열: grid-cols-4 gap-2 → grid-cols-2 sm:grid-cols-4 gap-1.5 sm:gap-2
Line 657 — AI 모델 3열: grid-cols-3 gap-2 → grid-cols-1 sm:grid-cols-3 gap-2
       (모델 셀 패딩): p-3 → p-2 sm:p-3
Line 748 — TTS 모델 2열: 유지 (OK at 360px)
Line 776 — TTS 음성 3열: grid-cols-3 gap-2 → grid-cols-2 sm:grid-cols-3 gap-1.5 sm:gap-2
```

---

### C-2. TravelLog.tsx — 헤더 + 컨트롤 행 + Day탭
**파일**: `src/pages/TravelLog.tsx`
```
Line 351 — 헤더: gap-3 → gap-2 sm:gap-3
Line 361 — 제목: text-xl → text-lg sm:text-xl
Line 395 — Day탭: gap-1.5 → gap-1 sm:gap-1.5
Line 436 — 컨트롤 행: flex items-center gap-1.5 mt-2 → flex items-center gap-1 sm:gap-1.5 mt-2 flex-wrap
```

---

### C-3. TripDetail.tsx — 액션 그리드 3열화 + 헤더 + 메타 정보
**파일**: `src/pages/TripDetail.tsx`
```
Line 199 — 헤더: gap-3 → gap-2 sm:gap-3
Line 205 — 제목: text-xl sm:text-2xl → text-lg sm:text-2xl (1단계 축소)
Line 209 — 헤더 버튼: gap-1.5 → gap-1 sm:gap-1.5
Line 288 — 메타 정보: gap-4 → gap-2 sm:gap-4
Line 313 — 모바일 액션: grid-cols-6 gap-1.5 → grid-cols-3 gap-2
       (액션 셀 min-h): min-h-[56px] → min-h-[48px]
       (액션 아이콘): size-5 → size-4
       (액션 텍스트): text-[10px] → text-[11px] (3열이므로 공간 여유)
```

---

### C-4. DayDetail.tsx — 제목 축소 + 헤더 gap + Day피커
**파일**: `src/pages/DayDetail.tsx`
```
Line 623 — 헤더: gap-4 → gap-2 sm:gap-4
Line 640 — Day 제목: text-2xl → text-xl sm:text-2xl
Line 642 — 부제: ml-2 → ml-1 sm:ml-2
Line 656 — 날짜+날씨: gap-3 → gap-2 sm:gap-3
Line 674 — 우측 버튼: gap-2 → gap-1 sm:gap-2
Line 905 (부근) — Day피커 그리드: 존재 시 grid-cols-7 → grid-cols-5 sm:grid-cols-7
```

---

### C-5. PlanForm.tsx — 헤더 래핑 + 좌표 + 사진 그리드 + 메모 헤더
**파일**: `src/pages/PlanForm.tsx`
```
Line 457 — 헤더 외부: flex items-center justify-between → flex flex-col sm:flex-row items-start sm:items-center justify-between gap-3
Line 458 — 좌측 flex: gap-4 → gap-2 sm:gap-4
Line 463 — 제목: text-2xl → text-xl sm:text-2xl
Line 473 — 백업 버튼: gap-2 → gap-1 sm:gap-2
Line 604 (부근) — 좌표 정보: gap-4 → gap-2 sm:gap-4
Line 642 (부근) — 좌표 입력: grid-cols-2 gap-3 → grid-cols-1 sm:grid-cols-2 gap-2 sm:gap-3
Line 854 (부근) — 메모 헤더: flex items-center justify-between → flex flex-wrap items-center justify-between gap-1
Line 964 (부근) — 사진 그리드: grid-cols-3 → grid-cols-2 sm:grid-cols-3 md:grid-cols-4
```

---

### C-6. PlanDetail.tsx — Google 정보 gap + 제목
**파일**: `src/pages/PlanDetail.tsx`
```
Line 167 — 제목: text-xl → text-lg sm:text-xl
Line 200 — 상세 제목: text-2xl → text-xl sm:text-2xl
Line 213 — Google 정보: gap-x-4 → gap-x-2 sm:gap-x-4
```

---

### C-7. TravelLogMap.tsx — 컨트롤 + Day필터
**파일**: `src/pages/TravelLogMap.tsx`
```
Line 89 — 제목: text-lg → text-base sm:text-lg
Line 95 — 컨트롤: gap-1.5 → gap-1 sm:gap-1.5
Line 161 — Day필터: gap-1.5 → gap-1 sm:gap-1.5
```

---

### C-8. NavigationView.tsx — 헤더 gap
**파일**: `src/pages/NavigationView.tsx`
```
Line 183 — 헤더: gap-4 → gap-2 sm:gap-4
Line 188 — 제목: text-xl → text-lg sm:text-xl
```

---

### C-9. PlaceLibrary.tsx — 필터 gap + 다이얼로그 헤더
**파일**: `src/pages/PlaceLibrary.tsx`
```
Line 320 (부근) — 헤더: gap-4 → gap-3 sm:gap-4
Line 468 (부근) — 다이얼로그 헤더 버튼: flex items-center gap-2
→ flex items-center gap-1 sm:gap-2 flex-wrap
Line 572 (부근) — 좌표 정보: gap-4 → gap-2 sm:gap-4
```

---

### C-10. SharedTrip.tsx — 메타 gap
**파일**: `src/pages/SharedTrip.tsx`
```
Line 243 — 메타 정보: gap-4 → gap-2 sm:gap-4
```

---

### C-11. TripMap.tsx — 헤더 gap + Day 필터
**파일**: `src/pages/TripMap.tsx`
```
Line 141 (부근) — 헤더: gap-4 → gap-2 sm:gap-4
Line 164 (부근) — Day필터: gap-1.5 → gap-1 sm:gap-1.5
```

---

### C-12. TripForm.tsx — 헤더 + 국가 정보 + 제목
**파일**: `src/pages/TripForm.tsx`
```
Line 194 (부근) — 헤더: gap-4 → gap-2 sm:gap-4
Line 202 (부근) — 제목: text-2xl → text-xl sm:text-2xl
Line 346 (부근) — 국가 정보: grid-cols-2 → grid-cols-1 sm:grid-cols-2
```

---

### C-13. Dashboard.tsx — 통계 카드 + 제목
**파일**: `src/pages/Dashboard.tsx`
```
Line 138 (부근) — 헤더: 유지 (이미 justify-between으로 양호)
Line 159 — 통계 그리드: 유지 (grid-cols-1 sm:grid-cols-3 이미 양호)
Line 234 — 여행 그리드: 유지 (grid-cols-1 md:grid-cols-2 이미 양호)
```

Dashboard는 이미 반응형이 잘 되어 있음. 제목 크기만 확인.

---

## D. 변경 패턴 요약

### 패턴 1: gap 축소 (가장 빈번)
```
gap-4  → gap-2 sm:gap-4
gap-3  → gap-2 sm:gap-3
gap-2  → gap-1.5 sm:gap-2
gap-1.5 → gap-1 sm:gap-1.5
```

### 패턴 2: 그리드 열 축소
```
grid-cols-5 → grid-cols-3 sm:grid-cols-5
grid-cols-4 → grid-cols-2 sm:grid-cols-4
grid-cols-6 → grid-cols-3
grid-cols-3 → grid-cols-1 sm:grid-cols-3 (카드형)
grid-cols-3 → grid-cols-2 sm:grid-cols-3 (선택형)
grid-cols-2 → grid-cols-1 sm:grid-cols-2 (폼 필드)
```

### 패턴 3: 제목 크기 축소
```
text-2xl → text-xl sm:text-2xl
text-xl → text-lg sm:text-xl
text-lg → text-base sm:text-lg
```

### 패턴 4: 밀집 행 래핑
```
flex items-center → flex items-center flex-wrap
flex justify-between → flex flex-col sm:flex-row (심한 경우)
```

### 패턴 5: 패딩 축소
```
p-6 → p-4 sm:p-6
p-4 → p-3 sm:p-4
p-3 → p-2 sm:p-3
```

### 패턴 6: 고정 크기 요소 축소
```
size-24 → size-20 sm:size-24
w-32 h-40 → w-24 h-32 sm:w-32 sm:h-40
p-12 → p-4 sm:p-12
E. 수정 대상 파일 전체 목록
#	파일	변경 수	주요 변경
기반 컴포넌트			
1	src/components/ui/Dialog.tsx	3	sizeStyles 모바일 전폭, p-6→p-4, actions gap
2	src/components/ui/Lightbox.tsx	1	p-12→p-4
3	src/components/layout/Header.tsx	2	좌/우 gap 축소
4	src/components/layout/PageContainer.tsx	1	p-4→p-3 sm:p-4
TravelLog 컴포넌트			
5	src/components/travellog/TimelineCard.tsx	4	헤더 wrap, 외부 gap, 영수증 사진
6	src/components/travellog/PhotoLogUploader.tsx	7	AI배너 스택, 썸네일, 드래그영역, 카드 gap
7	src/components/travellog/EditLogModal.tsx	4	그리드 단일열, 패딩, 버튼 gap
8	src/components/travellog/ExpenseSummary.tsx	2	통계 gap, 카테고리 gap
9	src/components/travellog/SearchFilterBar.tsx	1	칩 gap
페이지			
10	src/pages/Settings.tsx	12	그리드 열, 탭바, 셀 패딩, 제목
11	src/pages/TravelLog.tsx	4	헤더, 컨트롤, Day탭, 제목
12	src/pages/TripDetail.tsx	7	액션 3열, 헤더, 메타, 제목
13	src/pages/DayDetail.tsx	6	헤더, 제목, 날짜, 버튼, Day피커
14	src/pages/PlanForm.tsx	8	헤더 래핑, 좌표, 사진, 메모, 제목
15	src/pages/PlanDetail.tsx	3	제목 2곳, Google정보 gap
16	src/pages/TravelLogMap.tsx	3	제목, 컨트롤, Day필터
17	src/pages/NavigationView.tsx	2	헤더, 제목
18	src/pages/PlaceLibrary.tsx	3	헤더, 다이얼로그, 좌표
19	src/pages/SharedTrip.tsx	1	메타 gap
20	src/pages/TripMap.tsx	2	헤더, Day필터
21	src/pages/TripForm.tsx	3	헤더, 제목, 국가정보
22	src/pages/Dashboard.tsx	0	이미 양호 (확인만)
총계	~78개 변경	21개 파일
F. 검증 방법
빌드 확인: npm run build → TypeScript/CSS 에러 없음
Chrome DevTools Galaxy S24 시뮬레이션 (360×780):
모든 페이지 순회하며 체크:
가로 스크롤바 없음 (overflow-x 없음)
텍스트 잘림/겹침 없음
버튼 터치 타겟 44px 이상
모달이 화면 밖으로 넘치지 않음
페이지별 핵심 체크포인트:
Settings: 컬러 팔레트 3열, 이동수단 2열, AI모델 1열 확인
TravelLog: 컨트롤 버튼이 wrap되어 정렬되는지 확인
TripDetail: 액션 버튼 3열(2행) 그리드 확인
PlanForm: 헤더가 세로 스택으로 전환되는지 확인
Dialog: 모달이 360px에서 전폭 - 16px 여백 확인
Lightbox: 이미지 영역 최대화 확인
PhotoLogUploader: AI 배너가 세로 스택, 썸네일 축소 확인
태블릿/데스크톱 회귀 테스트: sm(640px) 이상에서 기존 레이아웃 완전 유지 확인
다크 모드: 축소된 요소들이 다크 모드에서도 정상 표시 확인
